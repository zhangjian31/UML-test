PreviewFragment->PreviewSession.init->startLoadInfo(InfoType.StickerCategoryList)-> stickerHandler.startLoadCategoryList();->
    stickerHandler->doStartLoadStickerCategoryListFromNetwork->
    StickerManager->startLoadStickerCategoryList->

    StickerManagerSAR->doStartLoadStickerCategoryListFromNetwork->
    	postNotifyFinishLoadStickerCategoryInfos
    	postNotifyFinishLoadStickerCategoryInfos
    handleDefaultStickerCategory
    	previewSession.setCurStickerCategory(categoryInfo);

PreviewSession
     startLoadSticker(stickerCategoryInfo.getStickerCategoryId());
     startLoadSticker(String stickerCategoryId)
stickerHandler.startLoadStickerList
	onLoadStickerList

if (firstLoad)
    mWrapperContent.getListener().onStickerListLoaded(stickerCategoryId, loadResult);

PreviewFragment 
	onStickerListLoaded
	addStickersMap
	updateSelectSticker
